"""
Unit tests for odm_linkmlgen.

One test module per source module under test. The tests cover the pure helper
functions, building small DataFrames inline rather than reading fixture files, so
that no Excel data dictionary is needed to run them.
"""
